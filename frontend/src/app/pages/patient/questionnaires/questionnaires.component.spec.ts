import { QuestionnairesComponent } from './questionnaires.component';

describe('QuestionnairesComponent', () => {
  it('should be defined', () => {
    expect(QuestionnairesComponent).toBeDefined();
  });
});
