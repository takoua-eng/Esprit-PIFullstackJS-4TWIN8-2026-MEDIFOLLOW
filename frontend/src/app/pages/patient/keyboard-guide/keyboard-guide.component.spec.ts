import { KeyboardGuideComponent } from './keyboard-guide.component';

describe('KeyboardGuideComponent', () => {
  it('should be defined', () => {
    expect(KeyboardGuideComponent).toBeDefined();
  });
});
