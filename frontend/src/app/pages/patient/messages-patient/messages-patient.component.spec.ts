import { MessagesPatientComponent } from './messages-patient.component';

describe('MessagesPatientComponent', () => {
  it('should be defined', () => {
    expect(MessagesPatientComponent).toBeDefined();
  });
});
